# VPS Quick Start - CommitCaster

Быстрый деплой бота на VPS за 5 минут!

## Требования

- VPS сервер (Ubuntu 20.04+, Debian 10+, или подобный)
- Доменное имя, указывающее на IP вашего VPS
- SSH доступ к серверу

## Шаг 1: Получите токены (перед деплоем)

### Telegram Bot Token
1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot`
3. Следуйте инструкциям
4. Скопируйте токен (например: `123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11`)

### Telegram Channel ID
1. Создайте публичный канал в Telegram
2. Добавьте вашего бота как администратора канала (дайте права на публикацию)
3. Скопируйте username канала (например: `@mychannel`)

### Groq API Key (бесплатно!)
1. Откройте [console.groq.com](https://console.groq.com)
2. Зарегистрируйтесь (можно через Google)
3. API Keys → Create API Key
4. Скопируйте ключ (начинается с `gsk_...`)

### GitHub Webhook Secret
Придумайте любую случайную строку, например:
```bash
openssl rand -hex 32
```
Или просто: `my_super_secret_webhook_key_123`

---

## Шаг 2: Подключитесь к VPS

```bash
ssh root@your-vps-ip
# или
ssh your-username@your-vps-ip
```

---

## Шаг 3: Клонируйте репозиторий

```bash
cd ~
git clone https://github.com/yourusername/CommitCaster.git
cd CommitCaster
```

---

## Шаг 4: Автоматический деплой

Запустите скрипт автоматического деплоя:

```bash
chmod +x deploy.sh
./deploy.sh
```

Скрипт спросит у вас:
1. **TELEGRAM_BOT_TOKEN** - введите токен от BotFather
2. **TELEGRAM_CHANNEL_ID** - введите @yourchannel
3. **GROQ_API_KEY** - введите ключ от Groq
4. **GITHUB_WEBHOOK_SECRET** - введите секретный ключ
5. **Домен** - введите ваш домен (например: bot.example.com)
6. **Email** для SSL - введите ваш email

Скрипт автоматически:
- Установит Docker, Nginx, Certbot
- Создаст .env файл
- Настроит Nginx
- Установит SSL сертификат
- Запустит бота

---

## Шаг 5: Настройте GitHub Webhook

После успешного деплоя:

1. Откройте ваш GitHub репозиторий
2. **Settings** → **Webhooks** → **Add webhook**
3. Заполните:
   - **Payload URL**: `https://your-domain.com/webhook/github`
   - **Content type**: `application/json`
   - **Secret**: ваш `GITHUB_WEBHOOK_SECRET`
   - **Which events**: Just the push event
4. Нажмите **Add webhook**

---

## Готово! 🎉

Теперь при каждом push в ваш репозиторий, бот будет публиковать пост в Telegram.

---

## Проверка работы

```bash
# Проверьте здоровье бота
curl https://your-domain.com/health

# Посмотрите логи
docker-compose -f docker-compose.single.yml logs -f

# Или используйте make
make logs
```

---

## Полезные команды

```bash
# Просмотр логов
make logs

# Обновление бота
make update

# Перезапуск
make docker-restart

# Проверка статуса
make docker-ps

# Проверка health
make health
```

---

## Если что-то пошло не так

### Проблема: SSL сертификат не установился

**Решение:**
```bash
# Убедитесь что домен указывает на IP вашего VPS
ping your-domain.com

# Попробуйте установить SSL вручную
sudo certbot --nginx -d your-domain.com
```

### Проблема: Бот не отвечает

**Решение:**
```bash
# Проверьте логи
docker-compose -f docker-compose.single.yml logs

# Проверьте что контейнер запущен
docker ps

# Перезапустите контейнер
docker-compose -f docker-compose.single.yml restart
```

### Проблема: GitHub webhook не работает

**Решение:**
1. Проверьте что домен доступен извне: `curl https://your-domain.com/health`
2. Проверьте Recent Deliveries в GitHub webhook settings
3. Убедитесь что секрет совпадает в `.env` и в GitHub webhook

### Проблема: Telegram не получает сообщения

**Решение:**
1. Убедитесь что бот добавлен как **администратор** канала
2. Проверьте права бота на публикацию сообщений
3. Проверьте правильность `TELEGRAM_CHANNEL_ID` (должен начинаться с `@`)

---

## Обновление бота

Когда выйдет новая версия:

```bash
cd ~/CommitCaster
./update.sh
```

Или:

```bash
git pull
docker-compose -f docker-compose.single.yml up -d --build
```

---

## Альтернатива: Без автоматического скрипта

Если вы хотите настроить всё вручную, следуйте [DEPLOYMENT.md](DEPLOYMENT.md)

---

## Стоимость

- **VPS**: от $5/месяц (Digital Ocean, Vultr, Hetzner)
- **Домен**: от $10/год (Namecheap, Cloudflare)
- **Groq API**: **БЕСПЛАТНО!** ✨
- **Telegram**: бесплатно
- **GitHub**: бесплатно
- **SSL (Let's Encrypt)**: бесплатно

**Итого**: ~$5-10/месяц

---

## Рекомендуемые VPS провайдеры

- [Digital Ocean](https://www.digitalocean.com/) - $5/месяц, простой
- [Vultr](https://www.vultr.com/) - от $2.5/месяц
- [Hetzner](https://www.hetzner.com/) - от €4/месяц, дешевый
- [Linode](https://www.linode.com/) - $5/месяц, надёжный

Минимальные требования: 1GB RAM, 1 CPU, 10GB диск

---

## Endpoints после деплоя

- `https://your-domain.com/health` - Health check
- `https://your-domain.com/webhook/github` - GitHub webhook
- `https://your-domain.com/swagger/index.html` - API документация

---

## Производительность

CommitCaster очень легковесный:
- RAM: ~20-50MB
- CPU: <1% при idle
- Диск: ~50MB

Один дешёвый VPS легко обработает тысячи webhook'ов в день.

---

**Создано с использованием Claude Code**
