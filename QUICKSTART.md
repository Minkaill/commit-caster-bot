# Быстрый старт CommitCaster

## Шаг 1: Получите токены (5 минут)

### 1. Telegram Bot Token

1. Откройте Telegram, найдите [@BotFather](https://t.me/BotFather)
2. Отправьте `/newbot`
3. Введите имя бота (например: "Мой Dev Блог")
4. Введите username (например: "my_dev_blog_bot")
5. Скопируйте токен (выглядит как `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

### 2. Telegram Channel

1. Создайте новый канал в Telegram (публичный или приватный)
2. Сделайте канал публичным и задайте username (например: "mydevblog")
3. Добавьте вашего бота как **администратора** канала
4. Для публичного канала используйте `@mydevblog`
   Для приватного - получите ID через [@username_to_id_bot](https://t.me/username_to_id_bot)

### 3. Groq API Key (БЕСПЛАТНО!)

1. Перейдите на [console.groq.com](https://console.groq.com)
2. Нажмите "Sign Up" (можно через Google)
3. После входа → "API Keys" → "Create API Key"
4. Скопируйте ключ (выглядит как `gsk_...`)

### 4. GitHub Webhook Secret

Просто придумайте любой секретный ключ:
```
my_super_secret_webhook_key_2024
```

## Шаг 2: Настройте .env

Скопируйте `.env.example` в `.env`:

```bash
cp .env.example .env
```

Отредактируйте `.env`:

```env
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
TELEGRAM_CHANNEL_ID=@mydevblog
GROQ_API_KEY=gsk_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
GITHUB_WEBHOOK_SECRET=my_super_secret_webhook_key_2024
PORT=8080
```

## Шаг 3: Запустите бота

```bash
# Установите зависимости
go mod download

# Запустите
go run cmd/bot/main.go
```

Должно появиться:
```
🚀 CommitCaster запущен на порту 8080
📡 Webhook URL: http://localhost:8080/webhook/github
```

## Шаг 4: Настройте webhook (с ngrok для теста)

### Установите ngrok

Windows:
```bash
# Скачайте с https://ngrok.com/download
# Или через choco:
choco install ngrok
```

Mac/Linux:
```bash
brew install ngrok
```

### Запустите ngrok

```bash
ngrok http 8080
```

Скопируйте HTTPS URL (например: `https://abc123.ngrok-free.app`)

### Настройте GitHub Webhook

1. Откройте ваш репозиторий на GitHub
2. **Settings** → **Webhooks** → **Add webhook**
3. Заполните:
   - **Payload URL**: `https://abc123.ngrok-free.app/webhook/github`
   - **Content type**: `application/json`
   - **Secret**: `my_super_secret_webhook_key_2024` (ваш секрет из .env)
   - **Which events**: "Just the push event"
4. **Add webhook**

## Шаг 5: Протестируйте!

1. Сделайте коммит в репозиторий:
   ```bash
   echo "test" > test.txt
   git add test.txt
   git commit -m "Добавил тестовый файл"
   git push
   ```

2. Проверьте логи бота - должно появиться сообщение о получении webhook

3. Через несколько секунд в вашем Telegram канале появится пост!

## Готово!

Теперь каждый push в GitHub будет автоматически создавать пост в вашем канале.

## Что дальше?

- Кастомизируйте промпт AI в `internal/services/ai.go`
- Деплойте на продакшен (Heroku, Railway, VPS)
- Добавьте поддержку нескольких репозиториев
- Настройте фильтры (игнорировать определённые ветки)

## Проблемы?

**Бот не публикует в канал:**
- Убедитесь что бот - администратор канала
- Проверьте правильность Channel ID

**Webhook не приходит:**
- Проверьте что ngrok запущен
- Проверьте что URL в GitHub webhook правильный
- Проверьте секрет

**AI не генерирует текст:**
- Проверьте Groq API ключ
- Убедитесь что есть интернет
